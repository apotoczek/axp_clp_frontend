/* Automatically transformed from AMD to ES6. Beware of code smell. */
import CashflowOverview from 'src/libs/components/analytics/CashflowOverview';

export default function(opts) {
    return new CashflowOverview(opts);
}
