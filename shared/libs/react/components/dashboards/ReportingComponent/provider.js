import BaseProvider from 'providers/base-provider';

export default class ReportingComponentProvider extends BaseProvider {
    static fromSelector = BaseProvider.fromSelector(ReportingComponentProvider);
}
