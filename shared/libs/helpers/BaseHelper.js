/* Automatically transformed from AMD to ES6. Beware of code smell. */
import Context from 'src/libs/Context';

export default function(opts, components) {
    let self = new Context(opts, components);

    return self;
}
